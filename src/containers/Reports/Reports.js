import React, { Component } from 'react';

import classes from './Reports.module.css';

export class Reports extends Component {
    render() {
        return (
            <div className={classes.Reports}>
                <h1>Pizza Reports</h1>
                <p>Currently does not implements</p>
            </div>
        );
    }
}

export default Reports;